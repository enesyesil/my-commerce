{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2020",                                /* Target JavaScript version (modern ECMAScript). */
    "module": "commonjs",                              /* CommonJS module system for Node.js compatibility. */
    "rootDir": "./src",                                /* Root directory of your TypeScript source files. */
    "outDir": "./dist",                                /* Output directory for compiled JavaScript files. */
    "strict": false,                                   /* Disable all strict type-checking options. */
    "skipLibCheck": true,                              /* Skip type checking of declaration files (improves compilation speed). */
    "esModuleInterop": true,                           /* Ensure compatibility with CommonJS and ES Modules. */
    "forceConsistentCasingInFileNames": true,          /* Enforce consistent casing in imports. */

    /* Type Checking */
    "noImplicitAny": false,                            /* Allow implicit 'any' types to reduce errors. */
    "noUnusedLocals": true,                            /* Enable error reporting for unused local variables. */
    "noUnusedParameters": false,                       /* Allow unused function parameters to reduce errors. */
    "noImplicitThis": false,                           /* Allow 'this' to have an implicit 'any' type. */
    "strictNullChecks": false,                         /* Disable strict null/undefined type-checking. */
    "allowSyntheticDefaultImports": true,              /* Allow default imports for modules without default exports. */

    /* Modules */
    "resolveJsonModule": true,                         /* Enable importing JSON files. */
    "typeRoots": ["./node_modules/@types"],            /* Specify type declaration file locations. */

    /* Emit Options */
    "declaration": true,                               /* Generate .d.ts files for TypeScript modules. */
    "sourceMap": true,                                 /* Generate source map files for debugging. */
    "removeComments": true,                            /* Remove comments from emitted JavaScript files. */
    "inlineSources": true,                             /* Include source files in source maps. */

    /* Node.js Specific */
    "moduleResolution": "node",                        /* Use Node.js module resolution. */
    "allowJs": true,                                   /* Allow JavaScript files to be compiled. */

    /* Completeness */
    "baseUrl": "./",                                   /* Base directory for non-relative module imports. */
    "paths": {
      "@/*": ["src/*"]                                 /* Configure path alias for easier imports. */
    }
  },
  "include": ["src/**/*"],                             /* Include all files in the src folder. */
  "exclude": ["node_modules", "dist"]                  /* Exclude output and dependency folders. */
}
